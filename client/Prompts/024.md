# Prompt 024 — Signature Upload Loader & Excel Modal Text Cleanup

**Date:** 2026-08-17  
**Scope:** Frontend (`client/`)

---

## User Requests

1. Show a loader in the Admin Digital Signature modal when uploading a signature (both in the empty state and change signature state).
2. Remove the "High-Capacity Batched Export" text banner from the Excel export modal.

---

## What Was Done

1. **Digital Signature Upload Loading State (`IdCardRequestComponents.tsx`)**:
   - In the empty state (`!ngoConfig.signatureUrl`), replaced static upload box with an animated spinner container reading `"Uploading digital signature… Please wait while the image is being saved"`.
   - In the existing signature preview container, displayed a spinner overlay and updated the "Change Signature" button to read `"Uploading…"` with spinner animation while disabling actions.

2. **Excel Export Modal Text Cleanup (`ExportExcelModal.tsx`)**:
   - Removed the "High-Capacity Batched Export" callout banner from the modal.

---

## Verification

- `npm run build` compiled cleanly with 0 errors in 396ms.
