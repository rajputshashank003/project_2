# Prompt 031 — Dynamic WhatsApp QR Link Resolution

**Date:** 2026-08-18  
**Scope:** Frontend (`client/`)

---

## User Requests

1. Support single server deployment by dynamically pointing the WhatsApp pairing link to the primary API server's `/qr` page.

---

## What Was Done

1. **`Navbar/index.tsx`**:
   - Updated `WHATSAPP_SERVICE_URL` to dynamically resolve from `VITE_API_BASE_URL` (stripping `/api/v1` and appending `/qr`) or falling back to `http://localhost:3000/qr`.

---

## Verification

- `npm run build` compiled with 0 errors in 325ms.
