# Prompt 009 — Manager Phone Setting in Organization Settings

**Date:** 2026-08-16  
**Scope:** Frontend (`client/`)

---

## User Request

> "create another org setting manager whihc store phone number and if someone request for donation or id then we should send a notification to that manager that someone xyz requested for x ₹ donation or id card, and link to website"

---

## What Was Done

1. **Type Definition Updates (`types/ngo.ts`)**:
   - Added `managerPhone?: string;` to `NgoConfig` interface.
2. **Context Defaults (`context/AppContext.tsx`)**:
   - Initialized `managerPhone: ''` in `DEFAULT_NGO_CONFIG`.
3. **API Request Layer (`utils/api_request/ngo.ts`)**:
   - Added `managerPhone: data.managerPhone` to `updateNgoConfig` request payload.
4. **Admin Settings Form (`screens/AdminSettings/components/AdminSettingsComponents.tsx`)**:
   - Added a dedicated `Manager Phone Number (for Instant WhatsApp Alerts)` field under `ContactSettingsForm`.
   - Included descriptive text explaining that new donation and ID card requests will trigger instant WhatsApp alerts to this number.
4. **Smooth Mobile Menu Drawer Animation (`index.css` & `Navbar/index.tsx`)**:
   - Designed custom `drawerSlideInRight` (`0.32s cubic-bezier(0.16, 1, 0.3, 1)`) and `backdropFadeIn` (`0.28s`) animations.
   - Applied `.animate-drawer-slide` and `.animate-backdrop-fade` to the mobile drawer and its backdrop, delivering an iOS-inspired smooth, non-cringe opening transition.

---

## Verification
- `npm run build` executed and passed with 0 errors.
