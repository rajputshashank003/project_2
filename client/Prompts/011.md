# Prompt 011 — Complete UI/UX Polish, Staged Uploads, Blood Group Management & SEO

**Date:** 2026-08-16  
**Scope:** Frontend (`client/`)

---

## User Requests

1. Staged uploads in Org Settings (Logo & Signature) and Gallery: do not upload immediately until user clicks Submit / Save.
2. Fix dual email/SMS notifications on donation and ID card approval/rejection.
3. Show ID Card and Certificate download buttons only after approval on admin screens.
4. Add loaders on ID Card and Certificate PNG & PDF download buttons; remove broken cache-bust query param.
5. Add blood group selection to onboarding modal, users table with filter, and profile page with editing.
6. Display dynamic impact statistics in Home & About pages editable in Org Settings.
7. Show photo captions directly on mobile screens in Gallery.
8. Fix Noticeboard jumping on slide changes.
9. Add skeleton loader on About page.
10. Add notice thumbnail preview on published notice cards in Admin Noticeboard.
11. Add loader on Add Slot button in Admin Team.
12. Polish Admin Event cards and edit modal.
13. Complete SEO meta tags in `index.html` and dynamic chunk retry in `App.tsx`.

---

## What Was Done

1. **Deferred Image Uploads & Staging**:
   - `AdminSettings`: Staged `logoFile` & `signatureFile` locally with preview URLs; uploaded together on "Save All Settings" without resetting unsaved inputs.
   - `AdminGallery`: Added image preview with remove button and dedicated "Submit / Upload Photo" button.

2. **Fixed Duplicate Emails**:
   - Removed redundant `notifyBoth` calls from `useAdminRequestDonation.ts` and `useAdminRequestIdCard.ts` (backend already sends emails in goroutine).

3. **Conditional Downloads & Canvas Loaders**:
   - `DonationRequestComponents.tsx` & `IdCardRequestComponents.tsx`: Set `showDownloadButtons` conditional on `approved` status.
   - `IDCardCanvas/index.tsx` & `CertificateCanvas/index.tsx`: Added download spinners (`isDownloadingPng`, `isDownloadingPdf`), removed `cacheBust: true`, and added mobile scroll/aspect-ratio wrappers.

4. **Blood Group Feature**:
   - `ProfileCompletionModal/index.tsx`: Added Blood Group dropdown selector and validation.
   - `AdminUsers.tsx` & `useAdminUsers.ts`: Added Blood Group table column and dropdown filter.
   - `UserProfile.tsx`: Added Blood Group badge and inline Profile Editor.

5. **Configurable Stats & Page Polish**:
   - `AdminSettingsComponents.tsx`: Added `StatsSettingsForm` for impact statistics.
   - `HomeComponents.tsx`: Rendered live stats and fixed noticeboard carousel layout height.
   - `About.tsx`: Added skeleton loader during initial config/data fetch.
   - `GalleryComponents.tsx`: Captions visible on mobile viewports.
   - `AdminNoticeboard.tsx`: Rendered thumbnail image in published notice cards.
   - `AdminTeam.tsx`: Added loader on "Add Slot" button.
   - `AdminEventComponents.tsx`: Polished event cards and form modal.

6. **SEO & App Reliability**:
   - `index.html`: Added meta tags, OpenGraph, Twitter cards, and Google Fonts.
   - `App.tsx`: Added `lazyWithRetry` to prevent stale chunk errors across deployments.

---

## Verification

- `npm run build` completed cleanly with 0 errors.
