# Prompt 018 — Add "Verify WhatsApp" Menu Option to Admin Menu

**Date:** 2026-08-17  
**Scope:** Frontend (`client/`)

---

## User Requests

1. Create a menu option in the admin menu named "Verify WhatsApp" that redirects to `VITE_WHATSAPP_SERVICE_URL` (`https://ngo-sandeep-whatsapp-service.onrender.com/qr`).

---

## What Was Done

1. **Environment Configuration (`.env` & `.env.example`)**:
   - Added `VITE_WHATSAPP_SERVICE_URL="https://ngo-sandeep-whatsapp-service.onrender.com/qr"`.

2. **Navigation Integration (`Navbar/index.tsx`)**:
   - Resolved `WHATSAPP_SERVICE_URL` from `import.meta.env.VITE_WHATSAPP_SERVICE_URL`.
   - Added a "Verify WhatsApp" link with `QrCode` and `ExternalLink` icons in both the Desktop Admin Dropdown and Mobile Admin Drawer.
   - Configured to open in a new tab (`target="_blank" rel="noopener noreferrer"`).

---

## Verification

- `npm run build` compiled cleanly in 345ms with 0 errors.
