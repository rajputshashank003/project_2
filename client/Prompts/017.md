# Prompt 017 — ID Card Payment Verification, 20-Item Button Pagination & Email Brand Palette

**Date:** 2026-08-17  
**Scope:** Frontend (`client/`) & Backend (`api/`)

---

## User Requests

1. Provide payment receipt verification in ID card approval workflow.
2. Add bottom button pagination (max 20 per page) and title page indicators across certificate admin, ID card admin, user management, and user profile tabs.
3. Ensure email color theme matches the website's brand emerald green palette without modifying text or structure.

---

## What Was Done

1. **ID Card Admin Payment Receipt Verification (`IdCardRequestComponents.tsx`)**:
   - Added dual navigation tabs to `IDCardPreviewModal`: "Payment Screenshot" and "ID Card Preview".
   - Defaulted to "Payment Screenshot" for pending requests and "ID Card Preview" for approved requests.
   - Updated Eye action button tooltip and handler to open payment verification preview.

2. **Universal Reusable Pagination Component (`client/src/components/Pagination/index.tsx`)**:
   - Built a component rendering item range text (`Showing 1 to 20 of X items`), numbered buttons (`1`, `2`, `3`...), active emerald highlight, and prev/next controls.

3. **20-Item Pagination & Title Page Indicators Across 5 Views**:
   - **Certificate Admin Page (`AdminRequestDonation`)**: Page size 20, bottom `<Pagination />`, and `Page X of Y` badge next to title.
   - **ID Card Admin Page (`AdminRequestIdCard`)**: Page size 20, bottom `<Pagination />`, and `Page X of Y` badge next to title.
   - **User Management Page (`AdminUsers`)**: Page size 20, bottom `<Pagination />`, and `Page X of Y` badge next to title.
   - **User Profile — My Donations Tab (`DonationList`)**: Page size 20, bottom `<Pagination />`, and `Page X of Y` indicator.
   - **User Profile — My ID Cards Tab (`IdCardList`)**: Page size 20, bottom `<Pagination />`, and `Page X of Y` indicator.

4. **Email Brand Theme (`donation_service.go`, `id_card_service.go`, `notification_service.ts`)**:
   - Updated rejection email header background to emerald brand color `#065f46` with emerald accents while preserving all text and variables.

---

## Verification

- `npm run build` in `client/` compiled cleanly with 0 errors in 296ms.
- `go build ./...` in `api/` compiled cleanly with 0 errors.
