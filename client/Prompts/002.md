bugs
1. the 91 is overlapping with placeholder fix this and fix all the input fields their icon is overlapping with placeholder and text 
2. and onhover is only works for desktop we have to create for mobile and tab also so these deatils should show always and same for delete button on cards at multiple places find all this type of places and fix those 
3. when we see next or prev in notice board in home screen it changes height which looks very bad we should make the height constant 

create an implementation plan 
Note: which i m going to review and after approval we are going to execute the plan
1. do read all the related files and flow, each and every related thing, variable, function file etc.
2. do not make any other flow changes, code is working fine we just have to change this
3. find bugs related to this flow if any fix those also
4. handle edge cases
5. do follow code rules and write rules
6. ask me relevant question if you want
7. create a perfect implementation plan, step by step, with brief explanation which i m going to review
8. Don’t assume anything by yourself, anything you think is not clear just ask me a question about that, don’t create anything by just assuming that this is possible as me each and every confusion you have about the task 
9. Use camel or snake case based on variables in that file which ever is max use that 
10. Put and divide things in components for fe and in utils for be to make it good code
11. Put variables in useXYZ context hook file of that particular component if used in 2 or more places to avoid prop drilling for fe
12. Use <React.Fragment> instead of <>
13. Create skeleton in different file for particular comp if required (fetching data from api)
14. use commom components as much as possible  


bugs include in plan 
1. note overlapping with address fix this 
2. admin can add thir digital signature image and put that on id card, first admin has to upload digital signature then only they can approve, and admin can change signature, delete signature, create mock api for this for now 
3. add certificate preview along with screenshot of payment 

include these bugs also 
1. downloading wrong pdf for id card, it should download like vertically from front then back 


include these bugs
1. fix this overlap of search bar also on all the pages 


make these also configurable with admin and add account number as payment option here 


