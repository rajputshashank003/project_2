# Prompt 026 — Donation Payment Details Skeleton Loader

**Date:** 2026-08-17  
**Scope:** Frontend (`client/`)

---

## User Requests

1. Show loader on the Payment Details card on the donation page until config details are fetched.

---

## What Was Done

1. **Integrated `isConfigLoading` into `useDonate.ts`**:
   - Destructured `isConfigLoading` from `useApp()` and exposed it via `useDonate()`.

2. **Added Skeleton Loading UI to `UpiDetails` (`DonateComponents.tsx`)**:
   - When `isConfigLoading` is true, displays animated skeleton placeholder cards matching the height and structure of the UPI, Phone, and Bank Transfer rows.
   - Smoothly reveals the configured payment details once the network fetch completes.

---

## Verification

- `npm run build` compiled cleanly with 0 errors in 281ms.
