# Prompt 016 — Redesign Rejection Email Templates & Display Rejection Reason

**Date:** 2026-08-16  
**Scope:** Frontend (`client/`) & Backend (`api/`)

---

## User Requests

1. Fix UI/UX for rejection emails for both Donation receipts and ID Card requests, and display the rejection reason entered by the admin.

---

## What Was Done

1. **Frontend (`notification_service.ts`)**:
   - Redesigned `buildDonationRejectionMessages` and `buildIdCardRejectionMessages` HTML bodies with a structured card container, dark branded header, rose-bordered reason callout box, and emerald CTA buttons linking directly to `/donate` and `/id-card/generate`.

2. **Backend (`donation_service.go` & `id_card_service.go`)**:
   - Retrieved `rejectionReason` and injected it into the email HTML and SMS body.
   - Built a branded, responsive email template for rejection notices across both services.

---

## Verification

- `npm run build` in `client/` compiled cleanly with 0 errors.
- `go build ./...` in `api/` compiled cleanly with 0 errors.
