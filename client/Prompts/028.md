# Prompt 028 — Payment Details & Donation Note on ID Card Generation Page

**Date:** 2026-08-17  
**Scope:** Frontend (`client/`)

---

## User Requests

1. Add donation details (UPI ID, Phone, Bank Account, IFSC) on top of the ID card generation page (`/id-generate`) like on the certificate/donation page.
2. Add a note: "Donate here to generate ID card and paste screenshot".

---

## What Was Done

1. **`useIDGenerate.ts` Hook Updates**:
   - Destructured `isConfigLoading` from `useApp()`.
   - Added clipboard copy handlers and active copied state flags (`copied`, `copiedPhone`, `copiedAccount`, `copiedIfsc`).
   - Exposed all copy utilities in the hook's return object.

2. **`IDPaymentDetails` Component (`IDGenerateComponents.tsx`)**:
   - Created the `IDPaymentDetails` component displaying the UPI ID, Phone, and Bank Account with individual Copy buttons.
   - Built a pulse skeleton loader active during `isConfigLoading` to avoid placeholder flash.
   - Added the donation instruction note: `"Donate here to generate ID card and paste screenshot below"`.

3. **Page Layout (`IDGenerate.tsx`)**:
   - Rendered `<IDPaymentDetails />` above `<IDForm />`.

---

## Verification

- `npm run build` compiled cleanly with 0 errors in 287ms.
