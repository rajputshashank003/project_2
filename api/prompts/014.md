# Prompt 014 — Redesign Rejection Email Templates & Display Rejection Reason

**Date:** 2026-08-16  
**Scope:** Backend (`api/`)

---

## User Requests

1. Fix UI/UX for rejection emails for both Donation receipts and ID Card requests, ensuring the rejection reason entered by the admin is clearly shown.

---

## What Was Done

1. **Donation Service (`donation_service.go`)**:
   - Extracted `rejectionReason` from `d.RejectionReason` (*string).
   - Fetched NGO Name dynamically from `orgRepo`.
   - Included rejection reason in SMS notification.
   - Built a branded, responsive HTML rejection email featuring a dark header with NGO Name, clear status badge, a prominent rose-bordered callout box displaying the rejection reason, and an emerald green CTA button linking directly to `/donate`.

2. **ID Card Service (`id_card_service.go`)**:
   - Extracted `rejectionReason` from `card.RejectionReason` (*string).
   - Fetched NGO Name dynamically from `orgRepo`.
   - Included rejection reason in SMS notification.
   - Built a branded, responsive HTML rejection email with the rejection reason callout box and an emerald green CTA button linking directly to `/id-card/generate`.

---

## Verification

- `go build ./...` compiled cleanly with 0 errors.
