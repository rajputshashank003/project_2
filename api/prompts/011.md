# Prompt 011 — Blood Group Field, Event Image Update Fix & Configurable Statistics

**Date:** 2026-08-16  
**Scope:** Backend (`api/`)

---

## User Requests

1. Add `blood_group` to user profile onboarding, allow editing in profile, display in users table, and add filtering by blood group.
2. Fix event update bug where deleting an image and saving caused duplicate images.
3. Add configurable organization impact statistics (`stat_beneficiaries`, `stat_volunteers`, `stat_events_held`, `stat_donations`, `stat_years_active`) to Org Settings.

---

## What Was Done

1. **Database Migration (`000015_add_blood_group_to_users.up.sql`)**:
   - Added `blood_group VARCHAR(10) NOT NULL DEFAULT ''` to `users` table.

2. **User Models & DTOs**:
   - `models/user.go`: Added `BloodGroup` field to `User` struct.
   - `dto/user_dto.go`: Added `BloodGroup` to `UpdateUserRequest` and `UpdateMyProfileRequest`.
   - `repository/user_repository.go`: Added `blood_group` query filter to `ListPaginated`.
   - `service/user_service.go` & `handler/user_handler.go`: Supported `BloodGroup` in list, user update, and profile update endpoints.

3. **Org Settings Statistics**:
   - `models/org_setting.go`: Added keys `SettingKeyStatBeneficiaries`, `SettingKeyStatVolunteers`, `SettingKeyStatEventsHeld`, `SettingKeyStatDonations`, `SettingKeyStatYearsActive` and DTO fields.
   - `dto/ngo_dto.go`: Added stat fields to `UpdateNgoConfigRequest`.
   - `service/ngo_service.go`: Handled saving and reading stat keys in Org Settings.

4. **Event Update Cascade Fix**:
   - `repository/event_repository.go` & `service/event_service.go`: Fixed `Update` method to update scalar fields (`Title`, `Description`, `UpdatedAt`) without GORM re-inserting stale preloaded associations.

---

## Verification

- `go build ./...` compiled cleanly with 0 errors.
