# Prompt 013 — User Profile Endpoint (`GET /api/v1/my/profile`)

**Date:** 2026-08-16  
**Scope:** Backend (`api/`)

---

## User Requests

1. Always pick user role (Admin / User) and designation from the database directly so that client-side role and designation badges remain synchronized with database truth.

---

## What Was Done

1. **UserService (`user_service.go`)**:
   - Added `GetMyProfile(id uuid.UUID) (*models.User, error)` to retrieve the current user record by ID from `userRepo.FindByID(id)`.

2. **UserHandler (`user_handler.go`)**:
   - Added `GetMyProfile(c *gin.Context)` to extract the authenticated user ID from context (`middleware.AuthUserIDKey`) and return the user's latest DB record.

3. **Routes (`routes.go`)**:
   - Registered `my.GET("/profile", userH.GetMyProfile)` in the `/api/v1/my` route group with authenticated user middleware.

---

## Verification

- `go build ./...` compiled cleanly with 0 errors.
