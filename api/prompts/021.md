# Prompt 021 — ID Card Contribution Amount & Database Migration

**Date:** 2026-08-17  
**Scope:** Backend (`api/`)

---

## User Requests

1. Add required contribution / donation amount option to ID Card request flow so that exported Excel spreadsheet shows the amount instead of being empty.

---

## What Was Done

1. **Database Migration (`000016_add_amount_to_id_cards`)**:
   - Added `000016_add_amount_to_id_cards.up.sql` (`ALTER TABLE id_cards ADD COLUMN IF NOT EXISTS amount NUMERIC(12, 2) NOT NULL DEFAULT 0;`).
   - Added `000016_add_amount_to_id_cards.down.sql` (`ALTER TABLE id_cards DROP COLUMN IF EXISTS amount;`).

2. **Model & DTO Updates**:
   - `models.IDCard`: Added `Amount float64` field (`gorm:"column:amount;not null;default:0" json:"amount"`).
   - `dto.CreateIDCardRequest`: Added `Amount float64` field (`json:"amount" form:"amount"`).

3. **Service & Notification**:
   - `id_card_service.go`: Set `Amount: req.Amount` on `card`.
   - Included applicant's declared amount in manager WhatsApp alert.

---

## Verification

- `go build ./...` compiled cleanly with 0 errors.
